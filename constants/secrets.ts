import Constants from 'expo-constants'

export const POKE_URL = Constants.manifest?.extra?.pokeUrl
export const SUPABASE_URL = Constants.manifest?.extra?.supabaseUrl
export const SUPABASE_PUBLIC_KEY = Constants.manifest?.extra?.supabasePublicKey

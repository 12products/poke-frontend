import Constants from 'expo-constants'

export const POKE_URL = Constants.manifest?.extra?.pokeUrl
export const SUPABASE_URL = Constants.manifest?.extra?.supabseUrl
export const SUPABASE_KEY = Constants.manifest?.extra?.supabaseKey
